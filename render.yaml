services:
  - type: web
    name: golf-team-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:10000 src.main:app
    plan: free
    envVars: []